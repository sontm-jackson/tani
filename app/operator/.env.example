# Production API origin (leave blank in dev — Vite proxies /api to localhost:4000).
# e.g. VITE_API_URL=https://api.tani.app
VITE_API_URL=

# Optional link to the farmer app shown in the header.
# Dev: VITE_PEER_URL=http://localhost:5174
VITE_PEER_URL=
