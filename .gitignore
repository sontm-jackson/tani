# dependencies & build output
node_modules/
dist/
dev-dist/
*.tsbuildinfo

# secrets — never commit (testnet keys, encryption key, etc.)
.env
.env.*
!.env.example

# local database (holds encrypted wallet keys)
*.db
*.db-journal

# pitch / planning / draft material — all kept local, not published
_local/
pitch-kit/
*.pptx

# misc
__pycache__/
.DS_Store
*.log

# local screenshot / debug artifacts
*.png
app/operator/shot.mjs
app/farmer/shot.cjs
